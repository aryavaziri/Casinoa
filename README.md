# Poker web-site with React and Django

To see the website, visit www.casinoa.nl
Use your own .env configurations to setup the application
Run `docker-compose up --build` to see messages in the terminal.

